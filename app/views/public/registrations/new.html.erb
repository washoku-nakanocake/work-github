<h2>新規会員登録</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <div>
    <label>名前</label>
    <div>
      (姓) <%= f.text_field :last_name %>
      (名) <%= f.text_field :first_name %>
    </div>
  </div>

  <div>
    <label>フリガナ</label>
    <div>
      (セイ) <%= f.text_field :last_name_kana %>
      (メイ) <%= f.text_field :first_name_kana %>
    </div>
  </div>

  <div><label>メールアドレス</label><%= f.email_field :email, placeholder: "sample@example.com" %></div>
  <div><label>郵便番号(ハイフンなし)</label><%= f.text_field :postal_code, placeholder: "0000000" %></div>
  <div><label>住所</label><%= f.text_field :address, placeholder: "東京都渋谷区代々木神園町0-0" %></div>
  <div><label>電話番号(ハイフンなし)</label><%= f.text_field :telephone_number, placeholder: "0000000000" %></div>
  <div><label>パスワード(6文字以上)</label><%= f.password_field :password %></div>
  <div><label>パスワード(確認用)</label><%= f.password_field :password_confirmation %></div>

  <div style="margin-top:12px;"><%= f.submit "新規登録", class: "btn btn-success" %></div>
<% end %>

<hr>
<p>既に登録済みの方 <%= link_to "こちら", new_customer_session_path %> からログインしてください。</p>